---
phase: 01-complete-prototype-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/DonorApp.jsx
autonomous: true
requirements:
  - FLOW-01
  - FLOW-03
  - BUG-01
  - UI-03

must_haves:
  truths:
    - "Donor Impact page renders a teal gradient impact card with total pledges, homes supported, residents helped, and giving streak pulled from mock data"
    - "Impact card shows 'Press & hold to save' hint and NeedFeed brand mark"
    - "DonorProfile notification toggles are wired to useState — clicking a toggle visually moves the thumb and changes the pill color"
    - "Exactly 3 toggles with BRD Section 3.7 labels: Email alerts, WhatsApp updates, Monthly impact summary"
    - "DonorPledges empty state renders icon + title + subtitle + Browse CTA"
    - "DonorBrowse no-results empty state renders icon + title + subtitle + Clear filters CTA"
    - "All navigation items, pledge flow modals, and tab toggles in Donor App work with mock data"
  artifacts:
    - path: "src/DonorApp.jsx"
      provides: "Wired notification toggles, shareable impact card, rich empty states, Donor App bug fixes"
      contains: "impactCard"
    - path: "src/DonorApp.jsx"
      provides: "Notification prefs useState object with 3 BRD-specified labels"
      contains: "Email alerts"
  key_links:
    - from: "src/DonorApp.jsx DonorImpact"
      to: "mock stats values (28, 9, 412, 5mo)"
      via: "hardcoded from existing DonorImpact stats block (line 659)"
      pattern: "impactCard|Pledges made"
    - from: "src/DonorApp.jsx DonorProfile"
      to: "prefs state object"
      via: "useState toggle handler"
      pattern: "prefs.*setPrefs|toggle.*label"
---

<objective>
Wire all interactive Donor App features: shareable impact card on the Impact page (FLOW-01), working notification preference toggles on the Profile page (FLOW-03), rich empty states in DonorPledges and DonorBrowse (UI-03), and a full interaction audit to fix any no-op buttons, broken tabs, and missing pledge flow states (BUG-01).

Purpose: Donors using the prototype must experience a fully interactive app. This plan delivers all donor-facing features except the Swiggy integration (Plan 04).
Output: Updated src/DonorApp.jsx with the impact card section, wired toggles, rich empty states, and all interaction bugs resolved.
</objective>

<execution_context>
@/Users/tusharbhatia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tusharbhatia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/ROADMAP.md
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/phases/01-complete-prototype-polish/01-CONTEXT.md
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/phases/01-complete-prototype-polish/01-RESEARCH.md
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/phases/01-complete-prototype-polish/01-UI-SPEC.md
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/phases/01-complete-prototype-polish/01-PATTERNS.md
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/phases/01-complete-prototype-polish/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: DonorImpact shareable card + DonorProfile wired notification toggles</name>
  <files>src/DonorApp.jsx</files>
  <read_first>
    - src/DonorApp.jsx — read the full DonorImpact component (line 639 onward, ~90 lines) and the full DonorProfile component (line 728 onward, ~80 lines); extract: existing stats values (total pledges, homes, residents helped, streak), existing toggle array labels and structure, how `go` prop is received
    - .planning/phases/01-complete-prototype-polish/01-UI-SPEC.md — New Components section 3 (Shareable Impact Card spec) and section 2 (Notification Toggles spec)
    - .planning/phases/01-complete-prototype-polish/01-PATTERNS.md — `DonorImpact shareable card` section and `DonorProfile notification toggles` section
    - src/index.css — confirm `--teal-dark`, `--teal`, `--radius-lg` token names (read :root block only)
  </read_first>
  <action>
    **DonorImpact — Add impact card section (per D-06, D-07).**
    Read the current `DonorImpact` component return statement. After the last existing element in the component (the top-homes card or chart), add a new `div` with className `"impactCard"` styled as follows:
    - `background: "linear-gradient(135deg, var(--teal-dark) 0%, var(--teal) 100%)"`
    - `borderRadius: "var(--radius-lg)"`, `padding: "32px 28px"` (normative exception per UI-SPEC)
    - `color: "#fff"`, `marginTop: 32` (xl token), `overflow: "hidden"`, `position: "relative"`

    Inside the card, build three sections:
    1. **Brand row** (flex, space-between): Left side — "NeedFeed" text in `var(--serif)` at 13px uppercase with `letterSpacing: "0.1em"` opacity 0.8, below it "My giving impact" at 12px opacity 0.6. Right side — brand mark widget: 36×36px rounded square (`borderRadius: 9`) with `background: "rgba(255,255,255,0.2)"`, display grid place-items center, serif "N" at 18px weight 600.
    2. **Stats grid**: `display: "grid"`, `gridTemplateColumns: "1fr 1fr"`, `gap: 20`, `marginBottom: 0`, `marginTop: 24`. Four stat cells: ["28", "Pledges made"], ["9", "Homes supported"], ["412", "Residents helped"], ["5mo", "Giving streak"]. Each cell: stat value in `var(--serif)` at `fontSize: 36`, `fontWeight: 500`, `lineHeight: 1.05`, `letterSpacing: "-0.02em"`; label below at `fontSize: 12`, `opacity: 0.7`, `marginTop: 4`.
    3. **Screenshot hint**: `"Press & hold to save"` at `fontSize: 11.5`, `opacity: 0.55`, `textAlign: "center"`, `marginTop: 24`.

    Stat values (28, 9, 412, 5mo) must come from the existing stats block in the same component — do not introduce a new data source. Read the component first to see the exact values used in the `.statsRow` block, then reference the same source.

    **DonorProfile — Wire notification toggles (per D-10, D-11).**
    Read the current DonorProfile component. Find the static toggle array (currently 5 items). Remove the static array entirely. Replace with:
    1. At the top of the DonorProfile component body, add: `const [prefs, setPrefs] = useState({ "Email alerts": true, "WhatsApp updates": true, "Monthly impact summary": false });` and `const toggle = (label) => setPrefs(p => ({ ...p, [label]: !p[label] }));`
    2. In the render where the toggles map, replace the static JSX with: `{Object.entries(prefs).map(([label, on]) => (<div key={label} className="row between" style={{padding:"4px 0"}}>` ... `</div>))}`
    3. The toggle pill must be a `<button>` element (not `<div>`) for accessibility: `<button onClick={() => toggle(label)} role="switch" aria-checked={on} aria-label={label}` styled as: `width:36, height:20, borderRadius:999, background: on ? "var(--teal)" : "var(--line)", border:"none", position:"relative", cursor:"pointer", transition:"all .15s"`.
    4. Thumb inside: `position:"absolute", top:2, left: on ? 18 : 2, width:16, height:16, borderRadius:"50%", background:"#fff", transition:"all .15s", boxShadow:"0 1px 2px rgba(0,0,0,0.15)"` — thumb is a non-interactive `div` inside the button.
    5. The `useState` import is already present at the top of DonorApp.jsx — do not add a duplicate import.
  </action>
  <verify>
    <automated>cd /Users/tusharbhatia/projects/NeedFeed_GSD && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "impactCard" src/DonorApp.jsx` returns at least one match with teal-dark gradient background
    - `grep -n "Press.*hold.*save\|hold to save" src/DonorApp.jsx` returns one match
    - `grep -n '"Email alerts"' src/DonorApp.jsx` returns exactly one match inside a `useState` initializer (not in a static array)
    - `grep -n '"WhatsApp updates"' src/DonorApp.jsx` returns one match
    - `grep -n '"Monthly impact summary"' src/DonorApp.jsx` returns one match
    - `grep -n 'role="switch"' src/DonorApp.jsx` returns at least one match on a toggle button element
    - The old static toggle labels ("New urgent need within 5km", "Weekly digest of nearby homes") do not appear in DonorApp.jsx — `grep -c "urgent need within 5km" src/DonorApp.jsx` returns 0
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>DonorImpact renders teal gradient card with 4 stats + hint. DonorProfile has 3 BRD-specified wired toggles with role="switch". Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Donor App empty states + full interaction bug audit (BUG-01)</name>
  <files>src/DonorApp.jsx</files>
  <read_first>
    - src/DonorApp.jsx — read lines 580–650 (DonorPledges component, empty state location, tab logic), lines 197–280 (DonorBrowse, filter logic, empty state location, `go` prop), lines 377–430 (PledgeForm steps — check all step transitions are wired), lines 430–550 (PledgeForm method/order/drop/success steps — look for no-op onClick handlers)
    - .planning/phases/01-complete-prototype-polish/01-UI-SPEC.md — New Components section 1 (empty state icon/title/sub/CTA table for each screen context)
    - .planning/phases/01-complete-prototype-polish/01-PATTERNS.md — `.empty` extension section (JSX pattern, CTA btn class)
    - src/index.css (confirmed in 01-01-SUMMARY.md) — confirm .empty .icon / .empty .title / .empty .sub classes are now present
  </read_first>
  <action>
    **Empty state: DonorPledges.** Find the location in the DonorPledges render where an empty list currently shows a plain string like `"No pledges in this tab yet."`. Replace with rich empty state JSX:
    - For "Active" tab empty state: icon `"🌱"`, title `"No pledges yet"`, sub `"Start by browsing homes near you."`, CTA button `.btn.teal.sm` label `"Browse homes"` onClick `() => go("browse")`.
    - For "Completed" tab empty state (if separate): icon `"🗂️"`, title `"No completed pledges"`, sub `"Your fulfilled pledges will appear here."`, no CTA (history context).
    If both tabs share one empty state check, use the active tab's version.

    **Empty state: DonorBrowse.** Find the location where filtered results === 0 shows a plain string like `"No homes match that filter."`. Replace with:
    - icon `"🔍"`, title `"No homes match"`, sub `"Try clearing your filters."`, CTA `.btn.outline.sm` label `"Clear filters"` onClick that resets `filter` state to `"all"` and `q` state to `""`.
    Note: the CTA uses `.outline` not `.teal` because it is a reset action, not a primary navigation action.

    **Interaction audit (BUG-01).** Read the pledge flow steps carefully. Fix every no-op onClick handler found. Known issues from RESEARCH.md:
    1. PledgeForm Step 2A "Order & Deliver" button currently calls `setStep("success")` directly — this will be replaced by the Swiggy MCP call in Plan 04. For now, ensure the step machine includes `"swiggy-loading"`, `"swiggy-error"`, and `"swiggy-success"` as valid step values in the `goBack` handler's else branch. Do NOT implement the actual MCP call yet — leave a clear `// TODO: SWIGGY MCP — implemented in Plan 04` comment in the order step handler.
    2. Check all tab toggle buttons in `DonorPledges` (Active / Completed): confirm each tab button has `onClick={() => setTab(tabValue)}` and the tab state variable drives the filter. If missing, wire them.
    3. Check all modal close buttons (pledge flow close/X button): confirm `onClick={onClose}` is present on every X button.
    4. Check `DonorPledges` "mark delivered" or status update buttons: confirm each has an onClick that updates the pledge status in local state (not just a visual indicator). If no status update logic exists, add `const [pledges, setPledges] = useState(MY_PLEDGES_SEED)` (or the equivalent) and wire the button to update status.
    5. Verify `DonorBrowse` → `DonorHomeProfile` navigation: clicking a home card calls `setSelectedHome(home); go("home-detail")` or equivalent. If the onClick is missing, add it using the pattern from the existing code.
    6. Verify `DonorHome` urgency cards navigate to the correct home — ensure `onClick` on urgency cards calls the same `openPledge` or `setSelectedHome` flow.
  </action>
  <verify>
    <automated>cd /Users/tusharbhatia/projects/NeedFeed_GSD && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"No pledges yet"' src/DonorApp.jsx` returns one match inside an `.empty` div with `.empty .title` child
    - `grep -n '"No homes match"' src/DonorApp.jsx` returns one match inside an `.empty` div with a "Clear filters" button
    - `grep -n 'TODO.*SWIGGY\|SWIGGY MCP' src/DonorApp.jsx` returns one match marking the Plan 04 integration point
    - `grep -c "goBack\|onClose" src/DonorApp.jsx` returns >= 3 (close handlers on pledge flow)
    - `npm run build` exits with code 0
    - No new npm packages were added (package.json is unchanged)
  </acceptance_criteria>
  <done>DonorPledges and DonorBrowse have rich empty states. All Donor App navigation, tabs, modals, and pledge flow transitions work. SWIGGY TODO comment marks Plan 04 entry point. Build clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DonorApp.jsx edits | All changes operate on mock data; no user input reaches external services in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | DonorProfile toggle state | accept | Session-state only per D-10; no persistence, no XSS vector |
| T-02-02 | Information Disclosure | Impact card stats | accept | All values are hardcoded mock data; no PII displayed |
| T-02-03 | Tampering | DonorBrowse filter reset | accept | Filter state is local useState; no external input involved |
| T-02-SC | Tampering | npm installs | accept | No new packages; existing lockfile unchanged |
</threat_model>

<verification>
After both tasks complete, start the dev server (`npm run dev`) and manually verify:
1. Navigate to Donor Impact page — teal gradient card visible with 4 stat cells and "Press & hold to save" hint
2. Navigate to Donor Profile — 3 toggle pills visible; clicking each one moves the thumb and changes color
3. Navigate to My Pledges (clear all pledges from mock if needed) — rich empty state with "No pledges yet" and "Browse homes" CTA visible
4. Navigate to Browse — change filter to a value with no results — rich empty state with "Clear filters" button visible
5. Click through all pledge flow steps — all modals open and close correctly
</verification>

<success_criteria>
- Shareable impact card renders on DonorImpact with correct layout, typography (serif Display-tier), and gradient colors
- Notification toggles: 3 toggles with exact BRD labels, each toggles independently with visual feedback (thumb moves, color changes)
- Empty states: DonorPledges and DonorBrowse display structured icon/title/sub/CTA markup using .empty sub-classes from Plan 01
- BUG-01: All tab toggles, modal close buttons, and pledge flow step transitions are wired and functional
- Build passes with no errors
</success_criteria>

<output>
Create `.planning/phases/01-complete-prototype-polish/01-02-SUMMARY.md` when done
</output>
