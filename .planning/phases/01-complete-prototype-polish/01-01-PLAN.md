---
phase: 01-complete-prototype-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - src/data.js
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - DESK-01
  - DESK-02

must_haves:
  truths:
    - "All screens use CSS token variables exclusively — no hex literals in new rules"
    - "Status badges render correct BRD colors for all statuses including Ordered"
    - "Every list view that can be empty renders icon + title + subtitle + CTA"
    - "All interactive elements show hover, focus, and active states"
    - "Urgency card grid uses minmax at 1024px so cards don't collapse below 200px"
    - "The Ordered pledge status exists in mock data and badge map"
  artifacts:
    - path: "src/index.css"
      provides: "Extended .empty sub-classes, .badge.ordered, global :focus-visible rule, .urgentRow responsive fix"
      contains: ".empty .icon"
    - path: "src/data.js"
      provides: "Ordered status in MY_PLEDGES_SEED, data shape for all new features"
      contains: "Ordered"
  key_links:
    - from: "src/index.css .empty"
      to: "DonorApp.jsx / AdminApp.jsx empty state JSX"
      via: ".empty .icon / .empty .title / .empty .sub CSS classes"
      pattern: "empty.*icon|empty.*title"
    - from: "src/data.js MY_PLEDGES_SEED"
      to: "DonorApp.jsx DonorPledges"
      via: "status: Ordered field"
      pattern: "Ordered"
---

<objective>
Lay the visual and data foundation for all Phase 1 features. This plan audits and corrects the CSS design token system, extends the `.empty` class with icon/title/subtitle/CTA sub-elements, adds the global `:focus-visible` rule, fixes desktop grid responsiveness at 1024px, adds the `Ordered` badge variant to match BRD Section 3.1.2, and seeds the mock data with an `Ordered`-status pledge example.

Purpose: All subsequent plans depend on these CSS classes and data shapes. Getting them right here prevents rework in Wave 2 plans.
Output: Updated `src/index.css` and `src/data.js` with no behavior changes visible yet — the tokens, CSS classes, and seed row are the foundation that Wave 2 plans wire up.
</objective>

<execution_context>
@/Users/tusharbhatia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tusharbhatia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/ROADMAP.md
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/phases/01-complete-prototype-polish/01-CONTEXT.md
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/phases/01-complete-prototype-polish/01-RESEARCH.md
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/phases/01-complete-prototype-polish/01-UI-SPEC.md
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/phases/01-complete-prototype-polish/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS foundation — empty states, focus ring, badge.ordered, desktop grid fix</name>
  <files>src/index.css</files>
  <read_first>
    - src/index.css — read fully before touching; locate: :root (lines 1–39), .empty (line 885), .badge variants (line 383–400), .btn hover/active rules, .urgentRow grid rule, existing :focus rules if any
    - .planning/phases/01-complete-prototype-polish/01-UI-SPEC.md — New Components section (empty state CSS, typography roles, spacing scale, interaction states table, desktop responsiveness contract)
    - .planning/phases/01-complete-prototype-polish/01-PATTERNS.md — `.empty` extension section (lines 405–457), shared patterns Token Usage section
  </read_first>
  <action>
    Make five targeted additions/edits to src/index.css. Do not touch any existing rules that are not explicitly listed — especially the legacy padding values documented in 01-UI-SPEC.md (`.main`, `.card`, `.empty`, `.detailHero`).

    **Addition 1 — Global focus rule.** Immediately after the existing `:root { }` block (before any component rules), insert:
    `:focus-visible { outline: 2px solid var(--teal); outline-offset: 2px; }` — this is the WCAG 2.1 AA focus indicator for all interactive elements.

    **Addition 2 — Empty state sub-classes.** Find the existing `.empty` rule (currently `text-align: center; padding: 40px 20px; color: var(--ink-3);`). Immediately after its closing brace, add three sub-class rules:
    - `.empty .icon { font-size: 32px; margin-bottom: 8px; opacity: 0.5; }` — Label/Small tier as emoji/icon, sm margin per normative scale
    - `.empty .title { font-family: var(--serif); font-size: 18px; color: var(--ink); font-weight: 500; margin-bottom: 8px; }` — Heading tier serif
    - `.empty .sub { font-size: 13px; color: var(--ink-3); margin-bottom: 16px; line-height: 1.5; }` — Label/Small tier, md margin

    **Addition 3 — .badge.ordered variant.** Find the block of `.badge.pledged`, `.badge.delivered`, `.badge.confirmed`, `.badge.missed` rules (around line 400–420). Immediately after the last status badge rule in that block, add:
    `.badge.ordered { background: var(--teal-light); color: var(--teal-dark); }` — same visual as Pledged per UI-SPEC color table.

    **Addition 4 — Desktop responsiveness fix.** Find the `.urgentRow` rule. Change its `grid-template-columns` value from `repeat(3, 1fr)` to `repeat(3, minmax(200px, 1fr))`. If the rule already has a media query context, preserve it. This prevents card collapse at 1024px viewport width where the available main area is ~784px.

    **Addition 5 — Btn disabled state.** Find `.btn[disabled]` or `.btn:disabled` rule. Confirm `opacity: 0.5; cursor: not-allowed;` is present. If missing, add it. This is needed for the Swiggy loading state in Plan 04.

    After all edits, run `npm run build` (or `vite build`) in the project root to confirm no CSS parse errors.
  </action>
  <verify>
    <automated>cd /Users/tusharbhatia/projects/NeedFeed_GSD && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "focus-visible" src/index.css` returns at least one match with `outline: 2px solid var(--teal)`
    - `grep -n "\.empty \.icon" src/index.css` returns a match with `font-size: 32px`
    - `grep -n "\.empty \.title" src/index.css` returns a match with `var(--serif)`
    - `grep -n "\.empty \.sub" src/index.css` returns a match with `font-size: 13px`
    - `grep -n "badge\.ordered\|\.badge\.ordered" src/index.css` returns a match with `var(--teal-light)`
    - `grep -n "urgentRow" src/index.css` returns a match containing `minmax(200px`
    - `npm run build` exits with code 0 (no CSS parse errors)
  </acceptance_criteria>
  <done>src/index.css has :focus-visible global rule, .empty sub-classes, .badge.ordered, minmax urgentRow, and .btn disabled state — build passes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Data foundation — Ordered status in MY_PLEDGES_SEED and StatusBadge map</name>
  <files>src/data.js, src/DonorApp.jsx</files>
  <read_first>
    - src/data.js — read fully; locate: MY_PLEDGES_SEED (or the array used by DonorPledges), DONOR_PLEDGES, HOMES shape, HISTORY shape, INCOMING_PLEDGES shape
    - src/DonorApp.jsx — lines 60–80 (StatusBadge map), lines 550–580 (MY_PLEDGES_SEED or pledge seed data), note the exact variable names and structure
    - .planning/phases/01-complete-prototype-polish/01-PATTERNS.md — `src/data.js` section (lines 461–482), StatusBadge extension note
    - .planning/phases/01-complete-prototype-polish/01-UI-SPEC.md — New Components section 5 "Ordered status" bullet
  </read_first>
  <action>
    Two targeted edits — one in `src/data.js` (if MY_PLEDGES_SEED is there) or `src/DonorApp.jsx` (if seed is inline), and one in `src/DonorApp.jsx` for the StatusBadge map.

    **Edit 1 — Add Ordered status to StatusBadge map.** In `src/DonorApp.jsx`, find the `const map = { ... }` object inside the `StatusBadge` component (around line 66–71). Add `"Ordered": "ordered"` as a new key-value pair. The class name `"ordered"` must match the `.badge.ordered` class added in Task 1.

    If `ABadge` in `src/AdminApp.jsx` has a parallel status map, add `"Ordered": "ordered"` there too. Read AdminApp.jsx lines 50–80 to check.

    **Edit 2 — Add one Ordered pledge row to seed data.** Find where `MY_PLEDGES_SEED` (or the equivalent seed array for the Donor Pledges tab) is defined. This may be in `src/data.js` or inline in `src/DonorApp.jsx`. Add one new pledge object at the end of the array with:
    - `status: "Ordered"` — the new status value
    - `item` value that matches something orderable on Swiggy (e.g., `"Toor Dal"`)
    - `method: "Order & Deliver"` — the delivery method that triggered the Swiggy flow
    - All other required fields (id, home, area, qty, unit, pledgeDate) matching the shape of existing rows
    - Do not change any existing seed rows.

    If `MY_PLEDGES_SEED` is in `src/data.js`, edit `src/data.js`. If it is inline in `src/DonorApp.jsx`, edit `src/DonorApp.jsx`.
  </action>
  <verify>
    <automated>cd /Users/tusharbhatia/projects/NeedFeed_GSD && grep -n '"Ordered"' src/DonorApp.jsx src/data.js 2>/dev/null | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '"Ordered"' src/DonorApp.jsx` returns at least one match (StatusBadge map entry) with value `"ordered"`
    - The pledge seed array contains exactly one row with `status: "Ordered"` and `method: "Order & Deliver"`
    - `npm run build` exits with code 0
    - No existing seed rows were removed or modified (verify by counting total rows before vs. after)
  </acceptance_criteria>
  <done>StatusBadge maps include "Ordered": "ordered" in DonorApp.jsx (and AdminApp.jsx if applicable), and one Ordered pledge exists in seed data. Build passes clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Codebase edit | Developer edits CSS/JS files; no external input crosses this boundary in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | index.css edit | accept | Pure local file edit; no external input; low risk |
| T-01-02 | Tampering | data.js / DonorApp.jsx seed data | accept | Mock data only; no user input in seed rows; no PII |
| T-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan; build uses existing lockfile |
</threat_model>

<verification>
After both tasks complete:
1. `npm run build` passes with exit code 0
2. `grep -c "focus-visible" src/index.css` returns >= 1
3. `grep -c "\.empty \.icon" src/index.css` returns >= 1
4. `grep -c '"Ordered"' src/DonorApp.jsx` returns >= 1
5. No existing CSS rules for `.empty`, `.btn`, `.badge`, or `.urgentRow` were deleted (only additions/modifications as specified)
</verification>

<success_criteria>
- src/index.css: :focus-visible rule present, .empty sub-classes present, .badge.ordered present, .urgentRow uses minmax(200px, 1fr)
- src/DonorApp.jsx (or src/data.js): StatusBadge map includes "Ordered" → "ordered", seed data has one Ordered pledge row
- Build passes clean — no regressions in existing styles
- No new npm packages installed
</success_criteria>

<output>
Create `.planning/phases/01-complete-prototype-polish/01-01-SUMMARY.md` when done
</output>
