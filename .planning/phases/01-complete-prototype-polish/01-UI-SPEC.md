---
phase: 1
slug: complete-prototype-polish
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-24
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Complete Prototype Polish.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.
> Source of truth for all executor decisions in this phase.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — custom CSS token system in `src/index.css` |
| Preset | not applicable |
| Component library | none — hand-rolled React components |
| Icon library | `src/Icon.jsx` (30+ icons already defined; add missing icons there only) |
| Font (sans) | Manrope (Google Fonts, already in index.html) |
| Font (serif) | Newsreader (Google Fonts, already in index.html) |
| Font (mono) | JetBrains Mono (Google Fonts, already in index.html) |

**No new npm packages are introduced in this phase.** No shadcn, no Tailwind, no CSS modules. All new styles go in `src/index.css` exclusively.

> Source: RESEARCH.md Standard Stack, index.css :root

---

## Spacing Scale

All spacing values for **new CSS rules** must use CSS token variables or the explicit pixel values listed in the normative scale below. No other pixel values may appear in new CSS rules.

### Normative Scale (use for all new rules)

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline chip padding |
| sm | 8px | Button gap, badge gap, form hint margin |
| md | 16px | Default element spacing, form field margin-bottom |
| lg | 24px | Section head margin-bottom, modal footer padding-top |
| xl | 32px | `.section` margin-top, impact card margin-top |
| 2xl | 48px | — |
| 3xl | 64px | Main content bottom padding |

**Exceptions (normative — declared in CONTEXT.md / RESEARCH.md):**
- Impact card padding: 32px 28px (CONTEXT.md D-07 / RESEARCH.md Pattern 5)
- Touch targets on toggle switches: 36px wide × 20px tall (RESEARCH.md Pattern 2)
- QR code block image: 160×160px fixed (CONTEXT.md D-08)

### Pre-existing Legacy Values — Do Not Replicate

These values exist in the codebase prior to this phase. They are frozen exceptions inherited from the pre-token codebase. Executors must not use these values for any new CSS rules; they are listed here only so the executor does not "fix" existing rules and introduce regressions.

| Context | Value | Non-multiple reason |
|---------|-------|---------------------|
| `.main` padding | 38px 44px 64px | 38px ≠ multiple of 4 |
| `.main.dense` padding | 28px 36px 56px | 36px ≠ multiple of 4 |
| `.card` padding | 18px | 18px ≠ multiple of 4 |
| `.modal` padding | 28px | 28px ≠ multiple of 4 (tolerated) |
| `.empty` padding | 40px 20px | 20px ≠ multiple of 4 |
| `.urgentCard` padding | 16px | OK — listed for completeness |
| `.detailHero` padding | 26px 28px | 26px ≠ multiple of 4 |

> Source: index.css :root + component rules; RESEARCH.md Patterns 2, 3, 5

---

## Typography

All type must use `var(--serif)` or `var(--sans)` — never raw font-family strings. No new font sizes may be introduced; use only the four roles declared below.

| Role | Family | Size | Weight | Line Height | Applies To |
|------|--------|------|--------|-------------|------------|
| Label / Small | sans | 12–13.5px | 500–600 | 1.3 | `.formField label`, `.tinyNote`, `.badge`, brand mark serif "N" (13px uppercase), screenshot hint, toggle label, small muted text |
| Body | sans | 14–15px | 400 | 1.45 | `body` default, form labels, general prose. Any 15px variant absorbs here. |
| Heading | serif / sans | 18–22px | 500–600 | 1.25 | `h2`, `h3`, `.sectionHead h2`, empty state titles, subheadings. Absorbs 17px, 18px, 20px, 22px into one tier. |
| Display | serif | 28–36px | 500 | 1.05–1.15 | `h1`, `.pageTitle` (28–32px), `.stat .value` and impact card stat numbers (34–36px). Shared display tier. |

**Weights used in this phase:** 400 (regular body) and 500–600 (medium/semibold labels, headings). No 700 or 300.

**Impact card stat numbers specifically:** `font-family: var(--serif); font-size: 36px; font-weight: 500; line-height: 1.05; letter-spacing: -0.02em` — role: Display — as specified in CONTEXT.md D-07 and RESEARCH.md Pattern 5.

**Empty state title:** `font-family: var(--serif); font-size: 18px; font-weight: 500` inside `.empty .title` — role: Heading.

**Size aliases (removed intermediate sizes — reference role name only):**
- 11.5px → Label/Small
- 12px → Label/Small
- 12.5px → Label/Small
- 13px → Label/Small
- 13.5px → Label/Small
- 17px → Heading
- 18px → Heading
- 20px → Heading
- 22px → Heading
- 28px–32px → Display
- 34px–36px → Display

> Source: index.css :root + h1/h2/h3/body rules; CONTEXT.md D-07; RESEARCH.md Pattern 1

---

## Color

All color values must use CSS custom properties. No hex literals in JSX or new CSS rules unless extending `:root`.

| Role | Variable | Hex | Usage |
|------|----------|-----|-------|
| Dominant (60%) | `var(--bg)` / `var(--surface)` | #FBFAF6 / #FFFFFF | Page background, card surface |
| Secondary (30%) | `var(--bg-2)` / `var(--line)` / `var(--line-2)` | #F5F4EE / #E6E9E6 / #EFF1EE | Sidebar, table headers, dividers, segGroup backgrounds |
| Accent — primary | `var(--teal)` | #1D9E75 | Active nav indicator, teal buttons, progress bars, toggle ON state, tab active underline, bar chart bars, focus ring |
| Accent — dark | `var(--teal-dark)` | #0F6E56 | Teal button hover, link text, active nav text, badge pledged text |
| Accent — light | `var(--teal-light)` / `var(--teal-tint)` | #E1F5EE / #F2FAF6 | Active nav background, pledged badge background, table row hover, share card background |
| Destructive | `var(--coral)` | #D85A30 | Missed/overdue badges, coral buttons (destructive CTAs), error inline messages |
| Warning | `var(--amber)` | #EF9F27 | Moderate urgency badges, delivered badges, partial badges |
| Success | `var(--green)` | #639922 | Confirmed/covered badges, checkmark success circle |
| Ink (primary text) | `var(--ink)` | #1B2421 | Primary body text, headings |
| Ink muted | `var(--ink-3)` | #6B7A75 | Secondary text, labels, empty state text |

**Accent reserved for (explicit list):**
1. Primary CTA button fill (`btn.teal`)
2. Active sidebar nav item (background + left indicator bar)
3. Active tab underline
4. Toggle switch ON state background
5. Focus ring (`outline: 2px solid var(--teal)`)
6. Progress bar fill (donation progress)
7. Bar chart bar color (Impact page monthly chart)
8. Impact card gradient background (`linear-gradient(135deg, var(--teal-dark) 0%, var(--teal) 100%)`)

**Status badge color map (BRD Section 3.1.2 — locked, do not override):**

| Status | Background variable | Text color |
|--------|-------------------|------------|
| Pledged | `var(--teal-light)` | `var(--teal-dark)` |
| Delivered | `var(--amber-light)` | `#b56e00` |
| Confirmed | `var(--green-light)` | `#4a7517` |
| Missed | `var(--coral-light)` | `var(--coral)` |
| Ordered (new) | `var(--teal-light)` | `var(--teal-dark)` — same as Pledged; add `.badge.ordered` |
| Urgent | `var(--coral-light)` | `var(--coral)` |
| Moderate | `var(--amber-light)` | `#b56e00` |

> Source: index.css :root color tokens; CONTEXT.md decisions D-01–D-11; BRD Section 3.1.2 via RESEARCH.md

---

## Visual Hierarchy

Primary focal point per primary screen. This does not prescribe design changes — it declares the existing visual priority so executors do not inadvertently de-emphasise the most important element.

| Screen | Primary Focal Point | Rationale |
|--------|--------------------|-----------| 
| DonorHome | Urgency strip — the urgent cards row at the top of the feed | Donors must see the most time-sensitive needs first; this row carries the highest visual weight via coral urgency badges and card shadows |
| AdminDashboard | Attention-required section — incoming pledges needing action | Admins open the dashboard to act; the pledges awaiting mark-received/delivered are the primary action surface |
| AdminNeedsBoard | Active needs table | The table is the complete list of what the home currently needs; it is the admin's primary management surface on this screen |

> Source: BRD screen layout specs; RESEARCH.md Architecture Patterns

---

## Component Inventory

These components already exist and must be reused. No parallel implementations.

| Component / Class | File | Purpose | Extend? |
|-------------------|------|---------|---------|
| `.btn` + `.teal`, `.outline`, `.ghost`, `.coral`, `.sm`, `.lg`, `.block` | index.css:344 | All CTA buttons | No — use as-is |
| `.badge` + urgency/status variants | index.css:383 | Status and urgency pills | Add `.badge.ordered` only |
| `.card` + `.hover` | index.css:280 | Card containers | No |
| `.empty` | index.css:885 | Empty state wrapper | Yes — add `.empty .icon`, `.empty .title`, `.empty .sub` sub-classes |
| `.segGroup` / `.tab` | index.css:511/891 | Tab toggles | No |
| `StatusBadge` / `ABadge` | DonorApp.jsx / AdminApp.jsx | Status badge React components | Add "Ordered" status mapping |
| `Icon.jsx` | src/Icon.jsx | SVG icon system | Add any missing icons (share, qr, cart) |
| `.modal` + `.overlay` | index.css:806 | Modal dialogs | No |
| `.shareCard` | index.css:858 | Share link card | No — reuse for QR block context |
| `.formGrid` | index.css:851 | Two-column form layout | No |
| `.progress` + `.bar` | index.css:785 | Progress bars | No |

> Source: CONTEXT.md code_context; RESEARCH.md anti-patterns

---

## Interaction States

All interactive elements must implement all four states listed. No element may be interactive without hover feedback.

| Element | Default | Hover | Focus | Active / Selected |
|---------|---------|-------|-------|-------------------|
| `.btn.teal` | `var(--teal)` fill | `var(--teal-dark)` fill, translateY(-1px) | `outline: 2px solid var(--teal); outline-offset: 2px` | translateY(0) |
| `.btn.outline` | transparent, `var(--line)` border | `var(--ink-3)` border | `outline: 2px solid var(--teal); outline-offset: 2px` | translateY(0) |
| `.btn[disabled]` | opacity 0.5, cursor not-allowed | no change | no outline | no change |
| `.navItem` | transparent bg | `var(--bg-2)` bg | `:focus-visible` ring | `var(--teal-light)` bg, teal left bar |
| `.tab` | transparent | `var(--ink)` text | `:focus-visible` ring | `var(--teal)` bottom border, `var(--ink)` text |
| `.urgentCard` / `.homeCard` | `var(--surface)`, `var(--line)` border | `var(--shadow)` shadow, border #d6dcd8 | `:focus-visible` ring | — |
| Toggle switch (notification prefs) | off = `var(--line)` bg, thumb at left | cursor pointer | `:focus-visible` ring on wrapper | on = `var(--teal)` bg, thumb at right; 0.15s transition |
| `.pill` | `var(--surface)`, `var(--line)` border | `var(--ink-3)` border | `:focus-visible` ring | `var(--ink)` bg, white text |
| Table row `.row:hover` | — | `var(--teal-tint)` bg | — | — |

**Global focus rule (add to index.css):**
```css
:focus-visible {
  outline: 2px solid var(--teal);
  outline-offset: 2px;
}
```

**Async confirmation pattern (mark-received, mark-delivered):**
- Button shows spinner or "Saving…" label during async mock delay (200ms setTimeout)
- On success: swap button with a green check + "Done" label, no page reload
- On error: show inline error text in `var(--coral)` below the button

> Source: REQUIREMENTS.md UI-04; RESEARCH.md Code Examples; WCAG 2.1 AA accessibility requirement

---

## New Components to Build

These do not exist yet and must be created in this phase.

### 1. Empty State Pattern (UI-03)

**CSS additions to index.css (extend `.empty`):**
```css
.empty .icon { font-size: 32px; margin-bottom: 8px; opacity: 0.5; }
.empty .title { font-family: var(--serif); font-size: 18px; color: var(--ink); font-weight: 500; margin-bottom: 8px; }
.empty .sub { font-size: 13px; color: var(--ink-3); margin-bottom: 16px; line-height: 1.5; }
```

Note: `margin-bottom` values use normative scale (8px = sm, 16px = md). Roles: `.empty .title` → Heading tier; `.empty .sub` → Label/Small tier.

**Icon choices per context (warm tone, brand-aligned):**
| Screen | Icon | Title | Subtitle | CTA |
|--------|------|-------|----------|-----|
| My Pledges (zero) | 🌱 | No pledges yet | Start by browsing homes near you. | "Browse homes" → `setActive("browse")` |
| Browse (no results) | 🔍 | No homes match | Try clearing your filters. | "Clear filters" → reset filter state |
| Needs Board / admin (zero needs) | 📋 | No needs listed | Add items your home needs from donors. | "Add first need" → open AddNeedForm |
| Incoming Pledges (zero) | 🤝 | No pledges yet | Share your home's QR code to attract donors. | "View QR code" → `setActive("profile")` |
| Donation History (zero) | 📊 | No history yet | Confirmed donations will appear here. | none |
| Pledge history (zero, filtered) | 🗂️ | No results | Try adjusting your date range or search. | "Reset filters" → clear filter state |

> Source: CONTEXT.md D-04, D-05; RESEARCH.md Pattern 1

### 2. Notification Toggles (FLOW-03)

Toggle switch styled inline — no new CSS class needed (uses existing inline style pattern).

**Layout:** `.row.between` with label at Label/Small tier (13.5px) and toggle pill (36×20px, border-radius 999px).
**States:** OFF = `var(--line)` background, thumb at `left: 2px`. ON = `var(--teal)` background, thumb at `left: 18px`. Thumb: 16×16px white circle, `border-radius: 50%`, `box-shadow: 0 1px 2px rgba(0,0,0,0.15)`. Transition: `all 0.15s`.
**Initial state:** Email alerts: ON, WhatsApp updates: ON, Monthly impact summary: OFF.
**Labels (BRD Section 3.7 — exactly 3, no others):** "Email alerts", "WhatsApp updates", "Monthly impact summary"

> Source: CONTEXT.md D-10, D-11; RESEARCH.md Pattern 2

### 3. Shareable Impact Card (FLOW-01)

**Container:** `linear-gradient(135deg, var(--teal-dark) 0%, var(--teal) 100%)`, `border-radius: var(--radius-lg)`, `padding: 32px 28px`, `color: #fff`, `margin-top: 32px`, `overflow: hidden`, `position: relative`.
**Brand row:** NeedFeed wordmark in `var(--serif)` at Label/Small tier (13px) uppercase + letter-spacing 0.1em, opacity 0.8. Brand mark: 36×36px rounded square, `rgba(255,255,255,0.2)` background, serif "N" at Label/Small tier (18px lower bound of Heading — use 13px for brand mark specifically as it is typeset at Label/Small scale within the brand mark widget).
**Stats grid:** 2×2 grid, gap 20px. Each cell: stat value in `var(--serif)` at Display tier (36px) weight 500 `letter-spacing: -0.02em`, label at Label/Small tier (12px) opacity 0.7.
**Stats shown:** Total pledges, Homes supported, Residents helped, Giving streak (pull from mock data).
**Screenshot hint:** "Press & hold to save" at Label/Small tier (11.5px end of range), opacity 0.55, centered, margin-top 24px.

> Source: CONTEXT.md D-06, D-07; RESEARCH.md Pattern 5

### 4. QR Code Block (FLOW-02)

**Placement:** Inside `HomeProfileEdit` (AdminApp.jsx:424), below the existing `.shareCard` form section.
**Container:** `.card` with `margin-top: 16px` (md token — normative scale).
**QR image:** `<img>` pointing to `https://api.qrserver.com/v1/create-qr-code/?data={encodeURIComponent(shareUrl)}&size=160x160`, 160×160px, `border-radius: 8px`, `border: 1px solid var(--line)`.
**Share URL format:** `https://needfeed.in/h/{slug}` where slug is derived from the home name.
**Copy button:** `.btn.outline.sm` with `Icon name="copy"` + "Copy link" label. On click: `navigator.clipboard.writeText(shareUrl)` with 2-second "Copied!" feedback on the button label.

> Source: CONTEXT.md D-08, D-09; RESEARCH.md Pattern 3

### 5. Swiggy Cart Integration — Step 2A UI (SWIGGY-01, SWIGGY-02)

**Entry point:** `PledgeForm` Step 2A — method selection "Order & Deliver" card.
**Button label:** "Add to Swiggy Cart" (`.btn.teal.block`)
**Loading state:** Button disabled + "Adding to cart…" label during MCP call sequence.
**Auth prompt (if no Swiggy token):** Inline card with Swiggy orange-tinted background: "Connect your Swiggy account to continue" + "Authenticate with Swiggy" link button.
**Success state:** Green check + "Added to Swiggy cart! Review your order." + `.btn.teal.block` labeled "Go to Swiggy" (opens `https://www.swiggy.com/instamart` in new tab with `noopener,noreferrer`). Below that: `.btn.outline.block` labeled "Mark as Ordered on NeedFeed" — clicking this advances pledge to "Ordered" status.
**Error state (inline):** `var(--coral)` text: "Could not add to Swiggy cart. [reason]" + `.btn.outline.sm` "Try again" retry button. Per CONTEXT.md D-02 — no automatic fallback.
**"Ordered" status:** Add `.badge.ordered` (same as pledged: `var(--teal-light)` bg, `var(--teal-dark)` text) to `StatusBadge` map and `ABadge` map. Add to `MY_PLEDGES_SEED` as a valid status.

> Source: CONTEXT.md D-01, D-02, D-03; RESEARCH.md Pitfalls 1–4, Pattern 4

---

## Desktop Responsiveness Contract (1024px+)

Minimum supported viewport: **1024px**. Mobile (< 1024px) is out of scope — do not add mobile breakpoints.

| Layout | Current rule | 1024px concern | Fix |
|--------|-------------|----------------|-----|
| `.urgentRow` | `repeat(3, 1fr)` | Cards narrow to ~244px at 1024px | Add `grid-template-columns: repeat(3, minmax(200px, 1fr))` |
| `.homeGrid` | `repeat(2, 1fr)` | Holds at 1024px | No change needed |
| `.twoCol` | `2fr 1fr` | Holds at 1024px | No change needed |
| `.attnRow` | `1fr 100px 110px 90px` | May overflow at 1024px | Add `overflow-x: auto` on parent `.tableCard` if needed |
| `.sidebar` | `width: 240px` fixed | Total 1024px: 240 sidebar + 784 main — acceptable | No change needed |
| `.main` padding | `38px 44px` (legacy value — do not replicate) | Fine at 1024px | No change needed |

**Breakpoint to add (if any grids need adjustment):**
```css
@media (max-width: 1100px) {
  .urgentRow { grid-template-columns: repeat(3, minmax(200px, 1fr)); }
}
```

> Source: RESEARCH.md Pitfall 7; REQUIREMENTS.md DESK-01, DESK-02

---

## Copywriting Contract

| Element | Screen | Copy |
|---------|--------|------|
| Primary CTA — Pledge | Pledge Flow Step 1 | "Confirm Pledge" |
| Primary CTA — Swiggy | Pledge Flow Step 2A | "Add to Swiggy Cart" |
| Primary CTA — Post-Swiggy | Pledge Flow Step 2A success | "Mark as Ordered on NeedFeed" |
| Primary CTA — Drop-off | Pledge Flow Step 2B | "Confirm Drop-off Date" |
| Primary CTA — Mark received | Admin Incoming Pledges | "Mark Received" |
| Primary CTA — Add need | Admin Needs Board | "Add Need" |
| Empty state — My Pledges | Donor Pledges tab | Title: "No pledges yet" / Sub: "Start by browsing homes near you." / CTA: "Browse homes" |
| Empty state — Browse results | Donor Browse | Title: "No homes match" / Sub: "Try clearing your filters." / CTA: "Clear filters" |
| Empty state — Needs Board | Admin Needs Board | Title: "No needs listed" / Sub: "Add items your home needs from donors." / CTA: "Add first need" |
| Empty state — Incoming Pledges | Admin Incoming Pledges | Title: "No pledges yet" / Sub: "Share your home's QR code to attract donors." / CTA: "View QR code" |
| Empty state — Donation History | Admin Donation History | Title: "No history yet" / Sub: "Confirmed donations will appear here." / CTA: none |
| Error — Swiggy failed | Pledge Flow Step 2A | "Could not add to Swiggy cart. [reason from MCP response]" / Sub-action: "Try again" |
| Error — Item not found | Pledge Flow Step 2A | "This item wasn't found on Swiggy Instamart in your area." / Sub-action: "Try again" |
| Error — Not serviceable | Pledge Flow Step 2A | "Swiggy doesn't deliver to this address yet." / Sub-action: "Try again" |
| Copy link feedback | Admin Home Profile QR | Button label: "Copy link" → "Copied!" (2 seconds) |
| Notification toggle labels | Donor Profile | "Email alerts", "WhatsApp updates", "Monthly impact summary" (exactly 3, per BRD Section 3.7) |
| Impact card brand label | Donor Impact | "NeedFeed" (uppercase, `var(--serif)`) |
| Impact card sub-label | Donor Impact | "My giving impact" |
| Impact card screenshot hint | Donor Impact | "Press & hold to save" |
| Destructive: delete need | Admin Needs Board — delete button | Confirm dialog: "Delete this need? This cannot be undone." / Buttons: "Cancel" (outline) + "Delete" (coral) |
| Destructive: no others | — | No other destructive actions in this phase |

> Source: CONTEXT.md D-04–D-11; REQUIREMENTS.md UI-03, FLOW-01–03, SWIGGY-01–02; BRD Section 3.7

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — shadcn not initialized | not applicable |
| Third-party | none | not applicable |

No npm packages are introduced. No registry safety gate required.

> Source: RESEARCH.md Package Legitimacy Audit

---

## Accessibility Contract

WCAG 2.1 AA is a BRD non-functional requirement. All elements must comply.

- **Focus:** `:focus-visible { outline: 2px solid var(--teal); outline-offset: 2px; }` — add globally to `index.css`.
- **Color + text:** All status badges use color AND dot + text label — color alone never conveys status.
- **ARIA:** All icon-only buttons must have `aria-label`. Toggle switches must have `role="switch"` and `aria-checked`.
- **Form inputs:** All inputs in PledgeForm must have associated `<label>`. Qty input: `type="number" min={1}`.
- **QR image:** `<img alt="Home QR code">`.
- **Swiggy redirect:** `window.open(url, "_blank", "noopener,noreferrer")`.
- **Modal:** On open, focus moves to modal; Escape key closes it. Modal has `role="dialog"` and `aria-labelledby`.

> Source: RESEARCH.md Security Domain + WCAG constraint; CLAUDE.md Accessibility requirement

---

## Security Constraints

| Pattern | Constraint | Implementation |
|---------|-----------|----------------|
| QR URL construction | XSS prevention | `encodeURIComponent(homeSlug)` before inserting into QR API URL |
| Qty input | Validation | `type="number"` with `min={1}` — already present; verify it is enforced |
| Swiggy redirect | Opener isolation | `window.open(url, "_blank", "noopener,noreferrer")` |
| Clipboard API | Graceful degradation | `navigator.clipboard.writeText()` with `execCommand('copy')` fallback |

> Source: RESEARCH.md Security Domain

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
