---
phase: 01-complete-prototype-polish
plan: 05
type: execute
wave: 4
depends_on:
  - 01-02
  - 01-03
  - 01-04
files_modified: []
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - FLOW-01
  - FLOW-02
  - FLOW-03
  - DESK-01
  - DESK-02
  - BUG-01
  - BUG-02
  - SWIGGY-01
  - SWIGGY-02

must_haves:
  truths:
    - "Developer has walked through every screen listed in the verification checklist"
    - "No visual regressions from the polish changes (fonts, colors, spacing)"
    - "All 6 phase success criteria from ROADMAP.md are met"
    - "Phase is signed off and ready for the next milestone"
  artifacts: []
  key_links: []
---

<objective>
Human review checkpoint: walk through the complete prototype to verify all 13 phase requirements are met, check visual quality and BRD fidelity, confirm Swiggy flow is demo-ready, and sign off the phase.

Purpose: Automated build verification cannot check visual polish, BRD color accuracy, interaction feel, or the Swiggy demo flow. Human eyes are required before the phase is marked complete.
Output: No files modified. Human confirms phase passes or files issues for gap-closure.
</objective>

<execution_context>
@/Users/tusharbhatia/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tusharbhatia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/ROADMAP.md
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/phases/01-complete-prototype-polish/01-CONTEXT.md
@/Users/tusharbhatia/projects/NeedFeed_GSD/.planning/phases/01-complete-prototype-polish/01-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Full prototype review — visual quality, interactions, and Swiggy flow</name>
  <what-built>
    Plans 01–04 delivered:
    - Plan 01: CSS foundation (empty state sub-classes, .badge.ordered, :focus-visible, .urgentRow minmax, data.js Ordered seed)
    - Plan 02: Donor App — shareable impact card, wired notification toggles, rich empty states, BUG-01 interaction fixes
    - Plan 03: Admin App — QR code block, EditNeedForm, DonationHistory search/filter/export, admin empty states, desktop overflow guard
    - Plan 04: Swiggy MCP integration — callSwiggyMCP, loading/error/success/Ordered states in PledgeForm
  </what-built>
  <how-to-verify>
    Start dev server: `cd /Users/tusharbhatia/projects/NeedFeed_GSD && npm run dev` then open http://localhost:5173

    **UI Polish (UI-01, UI-02):**
    - [ ] Open browser DevTools → Elements tab. Spot-check 5 elements across the app. Confirm all color values use CSS custom properties (var(--teal), var(--ink), etc.) — no raw hex in inline styles on new elements
    - [ ] Check status badges on Donor Pledges tab: Pledged = teal-light bg, Delivered = amber-light bg, Confirmed = green-light bg, Missed = coral-light bg, Ordered = teal-light bg (same as Pledged)
    - [ ] Verify BRD card layout: all card titles, status labels, and button labels match the copywriting contract in 01-UI-SPEC.md

    **Interactive feedback (UI-04):**
    - [ ] Hover over primary teal buttons — color darkens to teal-dark, slight translateY(-1px) lift
    - [ ] Tab through the app with keyboard only — focus ring (2px teal outline) visible on every focused element
    - [ ] Click "Mark Received" on an incoming pledge in Admin — button shows visual feedback before state update

    **Empty states (UI-03):**
    - [ ] Donor Pledges: temporarily remove all pledges from mock or switch to a tab with none — confirm rich empty state with emoji icon, serif title, subtitle, and CTA button
    - [ ] Admin Needs Board: temporarily comment out INITIAL_NEEDS or use in-app delete — confirm rich empty state
    - [ ] At least one admin empty state verified (Incoming Pledges or Donation History)

    **Impact card (FLOW-01):**
    - [ ] Navigate to Donor → Impact — scroll to bottom of page — teal gradient card visible with 4 stat cells
    - [ ] Stats show serif (Newsreader) font at large Display size with correct values (28 pledges, 9 homes, 412 residents, 5mo streak)
    - [ ] "Press & hold to save" hint visible below stats
    - [ ] NeedFeed brand mark (rounded square with "N") visible in top-right of card

    **QR code (FLOW-02):**
    - [ ] Navigate to Admin → Home Profile — scroll below the share link section — QR code image loads (or placeholder if offline)
    - [ ] If online: QR code img src points to api.qrserver.com with the home name encoded in URL
    - [ ] Click "Copy link" — button label changes to "Copied!" for ~2 seconds then reverts

    **Notification toggles (FLOW-03):**
    - [ ] Navigate to Donor → Profile — notification preferences section shows exactly 3 toggles: "Email alerts", "WhatsApp updates", "Monthly impact summary"
    - [ ] Click each toggle — thumb slides, pill background changes between teal and gray
    - [ ] Navigate away and return to Profile — toggles retain their state (session persistence)

    **Swiggy flow (SWIGGY-01, SWIGGY-02):**
    - [ ] Start a pledge on any home → choose "Order & Deliver" method → Step 2A shows single Swiggy pill + "Add to Swiggy Cart" button
    - [ ] Click "Add to Swiggy Cart" → auth prompt or loading state appears (not an instant redirect to old success)
    - [ ] The error state (expected for demo without real token) shows "Could not add to Swiggy cart." + "Try again" button — no fallback to other platforms
    - [ ] Confirm "swiggy-success" step is reachable (can force via browser console: type `setStep("swiggy-success")` if the component exposes it, or navigate via the demo state) — shows "Go to Swiggy" and "Mark as Ordered on NeedFeed"
    - [ ] "Go to Swiggy" opens a new tab to swiggy.com/instamart

    **Desktop responsiveness (DESK-01, DESK-02):**
    - [ ] Open DevTools → Responsive mode → set viewport to exactly 1024px × 768px
    - [ ] Scroll through Donor App: urgency cards row does not overflow, all 3 cards visible and readable
    - [ ] Scroll through Admin Dashboard: attention row table does not overflow horizontally (scrollable if tight)
    - [ ] No fixed-width elements break outside the 1024px viewport

    **Bug audit final check (BUG-01, BUG-02):**
    - [ ] Donor App: navigate every tab and screen — no dead-end buttons, no white screens
    - [ ] Admin App: click Edit on a NeedsBoard item — EditNeedForm opens inline with current values
    - [ ] Admin DonationHistory: type in search box — table filters; click Export CSV — file downloads
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checklist items pass.
    Or describe any specific failures (e.g., "Impact card font not serif", "Toggle doesn't animate") and Claude will create gap-closure plans.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser review | Read-only verification; no code changes in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Human review process | accept | Verification is read-only; no code changes; no attack surface |
| T-05-SC | Tampering | npm installs | accept | No packages installed in this plan |
</threat_model>

<verification>
This plan is itself the verification step. All automated checks were completed in Plans 01–04.
Final check: `cd /Users/tusharbhatia/projects/NeedFeed_GSD && npm run build` must exit 0 before human review begins.
</verification>

<success_criteria>
- Human has reviewed the complete prototype at 1024px and 1440px viewports
- All 6 ROADMAP Phase 1 success criteria are met as stated:
  1. Consistent spacing, color tokens, typography — no one-off values visible
  2. All cards, badges, buttons, modals match BRD visual spec (Section 3.1.2 and Section 4)
  3. All empty states are purposeful with meaningful messages
  4. All interactive elements have hover, focus, active feedback; status changes confirm visually
  5. Impact card shareable, QR code copyable, notification toggles persist in session, all screens usable at 1024px+
  6. "Add to Swiggy Cart" initiates MCP flow with item + quantity + address; donor redirected to Swiggy; "Ordered" state available on return
- Human types "approved" to close the phase
</success_criteria>

<output>
Create `.planning/phases/01-complete-prototype-polish/01-05-SUMMARY.md` when done
</output>
